sap.ui.define(['sap/uxap/BlockBase'],
    function(BlockBase) {
        "use strict";

        var BlockPhoneNumber = BlockBase.extend("onem.seya.ui5layout.object.view.blocks.personal.BlockPhoneNumber", {
            metadata: {}
        });


        return BlockPhoneNumber;

    });